# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'SamplePlayer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SamplePlayer
  pod 'Alamofire', '~> 4.7'
  pod "Himotoki", "~> 3.1"
  pod 'XCGLogger', '~> 6.0.2'
  pod 'Kingfisher', '~> 4.0'
  pod "RSLoadingView"
  pod 'YouTubePlayer'


end
